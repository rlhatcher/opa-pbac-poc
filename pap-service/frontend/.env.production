# Production Environment Configuration
# Base URL for PAP service API calls in production
VITE_PAP_SERVICE_URL=https://pap.yourdomain.com

# OPA server endpoint for Lambda backend in production
OPA_ENDPOINT=https://opa.yourdomain.com
